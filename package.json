{
  "name": "time-counter",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.4.0",

  "scripts": {
    "build": "turbo build",
    "deploy": "turbo preDeploy && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "turbo": "^2.0.3"
  }
}
